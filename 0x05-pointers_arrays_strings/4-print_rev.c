#include "main.h"

/**
* print_rev - prints a string in reverse
* @s: string tp be print in reverse
* Return: void
*/

void print_rev(char *s)
{
	int i;

	for (i = 0; s[i] !='\0'; i++)
	{
		continue;
	}
	s = i - 1;
	for (i = n; i >= 0; i--)
	{
		_putchar(s[i]);
	}
	_putchar("\n");
	return;
}
