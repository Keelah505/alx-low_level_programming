#include <stdio.h>
#include "main.h"

/**
 * mul - multiplies two digits
 * @a: the two int to multiply
 * @b: the two int to multiply
 * Return: result.
 */

int mul(int a, int b)
{
	return (a * b);
}

